import { LongPressDragDirective } from './long-press-drag.directive';

describe('LongPressDragDirective', () => {
  it('should create an instance', () => {
    const directive = new LongPressDragDirective();
    expect(directive).toBeTruthy();
  });
});
